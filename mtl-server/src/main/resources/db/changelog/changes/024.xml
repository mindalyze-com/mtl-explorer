<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.10.xsd">

    <!--
      Planner feature: introduce track_source column on gps_track.

      Existing tracks are imported from GPX files (recorded / Garmin / manual) and
      are marked as IMPORTED. The new PLANNED value flags synthetic routes created
      by the in-app route planner (BRouter-backed). Planned tracks are treated as
      "ideas" and must be excluded from statistics, duplicate detection, exploration
      scoring, activity classification and the main map layer by default.

      The distinction is enforced in two complementary places:
        (a) per-query filters in GpsTrackRepository native queries (hard-coded).
        (b) the central SmartBaseFilter SQL used by GpsTrackSQLFilter for client-facing
            filter-based listings (changeset in this same file, below).
    -->
    <changeSet id="20260418_1_track_source_column" author="patrick.heusser">
        <addColumn tableName="gps_track">
            <column name="track_source" type="varchar(32)" defaultValue="IMPORTED">
                <constraints nullable="false"/>
            </column>
        </addColumn>

        <createIndex indexName="GPS_TRACK_IX_TRACK_SOURCE" tableName="gps_track" unique="false">
            <column name="track_source"/>
        </createIndex>
    </changeSet>

    <!--
      Recreate gps_track_v view to expose the new track_source column.
      Required because the statistics native query (GpsTrackRepository.getTrackStatistics)
      reads from gps_track_v and must also filter out PLANNED rows.
    -->
    <changeSet id="20260418_2_view_track_source" author="patrick.heusser">

        <dropView viewName="gps_track_v"/>

        <createView viewName="gps_track_v">
            <![CDATA[
            SELECT gt.id,
                   gt.file_id,
                   gt.creator,
                   gt.gpx_version,
                   gt.meta_name,
                   gt.meta_description,
                   gt.meta_time,
                   gt.meta_author,
                   gt.track_name,
                   gt.track_description,
                   gt.start_date,
                   gt.end_date,
                   gt.track_duration_in_motion_secs,
                   gt.median_distance_between_points,
                   gt.avg_distance_between_points,
                   gt.max_distance_between_points,
                   gt.did_filter_outlier_by_distance,
                   gt.number_of_track_points,
                   gt.track_length_in_meter,
                   gt.track_type,
                   gt.utm_zone,
                   gt.create_date,
                   gt.load_status,
                   gt.load_messages,
                   gt.duplicate_status,
                   gt.duplicate_of,
                   gt.activity_type,
                   gt.activity_type_source,
                   gt.activity_type_source_details,
                   gt.garmin_activity_id,
                   gt.meta_link,
                   gt.update_date,
                   gt.duplicate_details,
                   gt.bbox_min_lat,
                   gt.bbox_max_lat,
                   gt.bbox_min_lng,
                   gt.bbox_max_lng,
                   gt.center_lat,
                   gt.center_lng,
                   gt.energy_gravitational_total_wh,
                   gt.energy_gravitational_descent_wh,
                   gt.energy_aero_drag_total_wh,
                   gt.energy_rolling_resistance_total_wh,
                   gt.energy_kinetic_positive_total_wh,
                   gt.energy_net_total_wh,
                   gt.energy_weight_kg_used,
                   gt.power_watts_avg,
                   gt.power_watts_max,
                   gt.source_segment_index,
                   gt.source_parent_track_id,
                   gt.exploration_status,
                   gt.exploration_score,
                   gt.exploration_calc_date,
                   gt.track_source,
                   ixf.index              AS f_index,
                   ixf.name               AS f_name,
                   ixf.path               AS f_path,
                   ixf.base_path          AS f_base_path,
                   ixf.full_path          AS f_full_path,
                   ixf.size               AS f_size,
                   ixf.indexer_id         AS f_indexer_id,
                   ixf.last_modified_date AS f_last_modified_date,
                   ixf.index_added_date   AS f_index_added_date,
                   ixf.index_update_date  AS f_index_update_date,
                   ixf.indexer_status     AS f_indexer_status,
                   ixf.last_message       AS f_last_message,
                   gd.id                  AS gd_id,
                   gd.create_date         AS gd_create_date,
                   gd.precision_in_meter  AS gd_precision_in_meter,
                   gd.track_type          AS gd_track_type,
                   gd.track               AS gd_track,
                   gd_raw.id              AS gd_raw_id,
                   gd_raw.track           AS gd_raw_track
            FROM gps_track gt
                     LEFT JOIN indexed_file ixf ON gt.file_id = ixf.id
                     LEFT JOIN gps_track_data gd
                               ON gt.id = gd.gps_track_id AND gd.precision_in_meter = 1 AND
                                  gd.track_type = 'SIMPLIFIED'
                     LEFT JOIN gps_track_data gd_raw
                               ON gt.id = gd_raw.gps_track_id AND gd_raw.precision_in_meter = 1 AND
                                  gd_raw.track_type = 'CLEANED'
            ]]>
        </createView>

    </changeSet>

    <!--
      Extend the SmartBaseFilter expression to exclude PLANNED tracks by default.
      This is the single source of truth used by every client-facing filter listing
      (map GeoJSON, stats page, exploration, track lists). Planner UI has its own
      endpoints and does NOT use this filter.
    -->
    <changeSet id="20260418_3_smart_base_filter_exclude_planned" author="patrick.heusser">
        <update tableName="filter_config">
            <column name="expression">
                <![CDATA[
                SELECT
                    id,
                    activity_type AS grp
                FROM
                    gps_track gt
                WHERE
                    1 = 1
                    -- OPTIONAL DATE_FROM and DATE_TO params. If NULL, they won't be applied as COALESCE uses start_date as fallback.
                    AND gt.start_date >= COALESCE(TO_TIMESTAMP(:DATE_TIME_FROM, 'YYYY-MM-DD HH24:MI:SS'), gt.start_date)
                    AND gt.start_date <= COALESCE(TO_TIMESTAMP(:DATE_TIME_TO, 'YYYY-MM-DD HH24:MI:SS'), gt.start_date)
                    AND gt.start_date >= '1971-01-01'
                    AND gt.duplicate_status = 'UNIQUE'
                    AND gt.load_status = 'SUCCESS'
                    -- Planner: exclude planned routes (ideas) — they are never "done" tracks.
                    AND gt.track_source = 'IMPORTED'
                    -- OPTIONAL GEO CIRCLE: if GEO_CIRCLE_1_POINT is NULL, condition is skipped
                    AND (
                        :GEO_CIRCLE_1_POINT IS NULL
                        OR EXISTS (
                            SELECT 1 FROM gps_track_data gtd
                            WHERE gtd.gps_track_id = gt.id
                              AND gtd.precision_in_meter = 1000
                              AND gtd.track_type = 'SIMPLIFIED'
                              AND ST_DWithin(
                                    gtd.track::geography,
                                    ST_GeographyFromText(:GEO_CIRCLE_1_POINT),
                                    CAST(:GEO_CIRCLE_1_RADIUS AS float8)
                                  )
                        )
                    )
                    -- OPTIONAL GEO RECTANGLE: if GEO_RECTANGLE_1_MIN_LAT is NULL, condition is skipped
                    AND (
                        :GEO_RECTANGLE_1_MIN_LAT IS NULL
                        OR EXISTS (
                            SELECT 1 FROM gps_track_data gtd
                            WHERE gtd.gps_track_id = gt.id
                              AND gtd.precision_in_meter = 1000
                              AND gtd.track_type = 'SIMPLIFIED'
                              AND ST_Intersects(
                                    gtd.track,
                                    ST_MakeEnvelope(
                                        CAST(:GEO_RECTANGLE_1_MIN_LNG AS float8),
                                        CAST(:GEO_RECTANGLE_1_MIN_LAT AS float8),
                                        CAST(:GEO_RECTANGLE_1_MAX_LNG AS float8),
                                        CAST(:GEO_RECTANGLE_1_MAX_LAT AS float8),
                                        4326
                                    )
                                  )
                        )
                    )
                    -- OPTIONAL GEO POLYGON: if GEO_POLYGON_1 is NULL, condition is skipped
                    AND (
                        :GEO_POLYGON_1 IS NULL
                        OR EXISTS (
                            SELECT 1 FROM gps_track_data gtd
                            WHERE gtd.gps_track_id = gt.id
                              AND gtd.precision_in_meter = 1000
                              AND gtd.track_type = 'SIMPLIFIED'
                              AND ST_Intersects(
                                    gtd.track,
                                    ST_GeomFromText(:GEO_POLYGON_1, 4326)
                                  )
                        )
                    )
                ORDER BY
                    start_date
                ]]>
            </column>
            <where>filter_name = 'SmartBaseFilter' AND filter_domain = 'GPS_TRACK'</where>
        </update>
    </changeSet>

</databaseChangeLog>
